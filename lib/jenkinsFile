pipeline {
    agent any
    tools {
        nodejs 'Node 20.11.1'  // This should match the name of the Node.js tool in Global Tool Configuration
    }
    stages {
        stage('Checkout') {
            steps {
                script {
                    checkout([$class: 'GitSCM', 
                        branches: [[name: '*/main']],
                        userRemoteConfigs: [[url: 'https://github.com/SrSousa011/GraceLink.git']]
                    ])
                }
            }
        }
        
        stage('Build') {
            steps {
                bat 'npm install'
                bat 'npm run build'
            }
        }

        stage('Run Unit Tests') {
            steps {
                bat 'npm run test'
            }
        }
    }
}
