pipeline {
    agent any

    options {
        buildDiscarder(logRotator(numToKeepStr: '5'))
    }

    environment {
        SONARQUBE_SERVER = 'sq1' // SonarQube server
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/SrSousa011/GraceLink.git'
            }
        }

        stage('Build') {
            steps {
                script {
                    bat 'mvn clean package'  // Build with Maven
                }
            }
        }

        stage('SonarQube Scan') {
            steps {
                withSonarQubeEnv(SONARQUBE_SERVER) {
                    script {
                        // Maven SonarQube analysis using the sonar:sonar goal
                        bat 'mvn sonar:sonar -Dsonar.projectKey=gracelink -Dsonar.sources=lib -Dsonar.tests=test -Dsonar.language=dart'
                    }
                }
            }
        }

        stage('Quality Gate') {
            steps {
                script {
                    def qualityGate = waitForQualityGate()

                    if (qualityGate.status != 'OK') {
                        echo "Quality Gate failed: ${qualityGate.status}"
                        currentBuild.result = 'ABORTED'
                        return
                    }
                }
            }
        }
    }

    post {
        always {
            script {
                echo "Performing post-build actions on Windows"
            }
        }

        success {
            script {
                echo "Build and SonarQube scan passed successfully!"
            }
        }

        failure {
            script {
                echo "Build failed. Check the logs for more details."
            }
        }
    }
}
